# Local Operations

This fixture library represents a small offline LOKI THE SUN GOD operator shelf.

## Startup

- The dashboard can run without `DISCORD_TOKEN`.
- Saved guild settings should still be readable from SQLite.

## Diagnostics

- Diagnostics should report whether the database, docs, and runtime log exist.
- Ollama and 9router checks are optional and should fail soft.
