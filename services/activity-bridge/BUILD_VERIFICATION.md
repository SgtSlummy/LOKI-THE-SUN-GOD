# Build verification

The starter was generated and verified with:

```bash
npm install --ignore-scripts
npm run build
```

Result:

```text
shared: TypeScript build passed
client: TypeScript build passed and Vite production build passed
server: TypeScript build passed
```

`node_modules/` and generated `dist/` folders are intentionally excluded from the export zip.
