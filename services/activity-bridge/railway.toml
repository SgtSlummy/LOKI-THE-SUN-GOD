[build]
builder = "RAILPACK"
buildCommand = "npm run build"

[deploy]
startCommand = "sh -c 'if [ \"$LOKI_ACTIVITY_SERVICE_ROLE\" = \"client\" ]; then npm run start:client; else npm run start; fi'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
